# Tasks

- [x] 1. Promote `EnqueueOutcome` to enum class
- [x] 2. Promote `TransferPreference` to enum class
- [x] 3. Promote `SubtitleEnqueueOutcome` to enum class
- [x] 4. Promote `DownloadFailureReason` to enum class
- [x] 5. Promote `DownloadCommandOutcome` to enum class
- [x] 6. Update `is X` callers in `SeasonQueueController` and `DownloadsScreen`
- [x] 7. Run pre-commit suite (spotless, detekt, jvmTest, Android/D/Web builds)
- [x] 8. Open PR, address review, merge
- [x] 9. Archive OpenSpec change
